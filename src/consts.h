#ifndef CONSTS_H
#define CONSTS_H

#define BOTTOM_MENU_HEIGHT 2
#define ARRAY_SIZE(arr) (sizeof(arr) / sizeof(arr[0]))
#define TMP_EXT ".tmp"
#define MIN(a,b) ((a) < (b) ? (a) : (b))

#endif // CONSTS_H
